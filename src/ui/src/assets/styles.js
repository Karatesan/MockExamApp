import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: "#7518ff",
            contrast: "#FFF"
        }
    },
    typography: {
        primary: {
            color: "white",
            fontSize: 16
        }
    }
})